{
  "name": "rux.web",
  "version": "0.0.1",
  "main": "index.js",
  "scripts": {
    "watch": "sass --watch --update --style=expanded Assets/Styles:wwwroot/css",
    "build": "sass --no-source-map --style=compressed Assets/Styles:wwwroot/css",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Ivan Muzyka",
  "license": "MIT",
  "description": "https://rux-lang.dev",
  "devDependencies": {
    "sass": "^1.83.0"
  }
}
